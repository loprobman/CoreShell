# pkg

## Usage

```
pkg <subcommand> [args...]
```

## Options

```
Subcommands:
  build <src-dir> <output.tar.gz>
  install <archive.tar.gz>
  list
  remove <name>
  check-update <name>
  upgrade <name>
  compile [--dry-run] [dir]
```
